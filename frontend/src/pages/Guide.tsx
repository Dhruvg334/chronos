import { Link } from 'react-router-dom';

export default function Guide() { return <div className="min-h-screen bg-canvas"><header className="page-container flex h-16 items-center justify-between"><Link to="/" className="text-xl font-semibold">Chron<span className="text-accent">OS</span></Link><Link to="/demo" className="text-sm font-medium text-muted">Guided demo</Link></header><main className="page-container py-12"><div className="mx-auto max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-accent-strong">Guide</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">From scattered commitments to a realistic day</h1><p className="mt-5 text-lg leading-8 text-muted">ChronOS combines what you need to do with the time you actually have. It recommends a next action, explains consequential suggestions, and waits for approval before external changes.</p><div className="mt-10 grid gap-4 sm:grid-cols-3">{[['Capture','Put commitments into Inbox without building a large form.'],['Plan','Place meaningful work around calendar reality and buffers.'],['Focus and adapt','Work the next action, then repair the plan when conditions change.']].map(([title,text], index) => <section key={title} className="surface p-5"><span className="text-sm font-semibold text-accent-strong">0{index + 1}</span><h2 className="mt-3 font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted">{text}</p></section>)}</div><section className="mt-10 surface-subtle p-6"><h2 className="text-lg font-semibold">Your approval boundary</h2><p className="mt-2 text-sm leading-6 text-muted">Reading internal context and preparing recommendations can happen automatically. Sending messages, deleting data, changing permissions, or writing calendar events requires explicit approval.</p></section></div></main></div>; }
