import { ArrowRight, CheckCircle2, Clock3, ShieldCheck } from 'lucide-react';
import { Link } from 'react-router-dom';
import { StrategyRecommendationCard } from '../components/strategy/StrategyRecommendationCard';

const plan = [
  { time: '09:00', title: 'Authentication regression fix', detail: '60 minutes · urgent and important' },
  { time: '10:15', title: 'Review deployment notes', detail: '25 minutes · batched review' },
  { time: '11:00', title: 'Prepare Monday slides', detail: '45-minute first focus interval' },
];

export default function Demo() { return <div className="min-h-screen bg-canvas"><header className="page-container flex h-16 items-center justify-between"><Link to="/" className="text-xl font-semibold">Chron<span className="text-accent">OS</span></Link><div className="flex items-center gap-3"><Link to="/guide" className="text-sm font-medium text-muted">Guide</Link><Link to="/signup" className="button-primary">Sign up</Link></div></header><main className="page-container py-10"><div className="mx-auto max-w-5xl"><div className="mb-8 max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-accent-strong">Guided demo · static and private</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">A busy day, made credible</h1><p className="mt-4 text-lg leading-8 text-muted">This public scenario uses static data. It does not authenticate, poll protected APIs, or access a workspace.</p></div><div className="grid gap-5 lg:grid-cols-[1.2fr_0.8fr]"><section className="surface p-6"><div className="flex items-center justify-between"><div><p className="text-sm font-medium text-success">Plan is workable after one deferral</p><h2 className="mt-2 text-2xl font-semibold">Finish the authentication fix</h2></div><CheckCircle2 className="h-6 w-6 text-success" /></div><ol className="mt-7 space-y-2">{plan.map(item => <li key={item.time} className="flex gap-4 rounded-xl bg-surface-subtle p-4"><span className="text-sm font-semibold text-accent-strong">{item.time}</span><div><h3 className="font-medium">{item.title}</h3><p className="mt-1 text-sm text-muted">{item.detail}</p></div></li>)}</ol><div className="mt-5 flex items-start gap-2 rounded-xl border border-line p-4 text-sm text-muted"><Clock3 className="mt-0.5 h-4 w-4 shrink-0" />The database assignment moves to a protected block tomorrow morning. Nothing changes automatically.</div></section><aside className="space-y-5"><section className="surface p-5"><div className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-accent" /><h2 className="font-semibold">One decision waiting</h2></div><p className="mt-3 text-sm leading-6 text-muted">Approve the explicit deferral, or choose a smaller slide scope. External calendars remain unchanged.</p></section><StrategyRecommendationCard recommendation={{ title: 'Constrain the day', why: 'The original work exceeds the 240 minutes available.', evidence: ['one 150-minute outcome', 'five short tasks', '240 free minutes'], action: 'Protect one major outcome and defer lower-value work explicitly.', tradeoff: 'Some work moves to tomorrow.', automaticChange: false }} /></aside></div><div className="mt-8 flex flex-col items-center justify-between gap-4 rounded-2xl bg-ink p-6 text-white sm:flex-row"><div><h2 className="text-lg font-semibold">Try the same loop with your commitments</h2><p className="mt-1 text-sm text-white/70">Capture, review, plan, and focus with clear approval boundaries.</p></div><Link className="inline-flex items-center rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-ink" to="/signup">Create an account<ArrowRight className="ml-2 h-4 w-4" /></Link></div></div></main></div>; }
