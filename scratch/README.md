# Scratch workspace

This directory is reserved for temporary local experiments that do not belong in the application, tests, migrations, or documentation.

Everything in `scratch/` is ignored by Git except this file. Do not place credentials, tokens, production data, database exports, or scripts that bypass repository and security boundaries here.

Reusable checks belong in `backend/tests`, `frontend/src/test`, or `scripts`.
